import { StyleSheet, Text } from "react-native";
const styles = StyleSheet.create({
  review: {
    fontSize: 30,
    fontFamily: "OpenSans-Regular",
  },
});
const DetaillPage = () => {
  return <Text style={styles.review}>Detail page gogle</Text>;
};


export default DetaillPage